This file holds the TestFileToMinimize.java original code. If you run AsheTest#mockTest(), TestFileToMinimize.java
should be overwritten. If you would like to run the tests again, just replace TestFileToMinimize.java content with
the content in this file.

------------------------------------------------------------------------------------------------------------------------

package ashe;

import java.io.IOException;
import java.io.OutputStream;
import java.net.Socket;

public class TestFileToMinimize {
    public void testSocket(int index) {
        String serverIp = "127.0.0.1";
        int serverPort = 8888;
        try {
            Socket socket = new Socket(serverIp, serverPort);
            OutputStream outputStream = socket.getOutputStream();
            String message = "Hello world";
            outputStream.write(message.getBytes());
            outputStream.flush();
        } catch (IOException ex) {
            throw new RuntimeException(ex);
        }
    }
}